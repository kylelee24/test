const path = require('path')

const SRC_PATH = path.join(__dirname, 'src')
const DATABASE_PATH = path.join(SRC_PATH, 'database')

module.exports = {
  'config': path.join(DATABASE_PATH, 'config', 'config.json'),
  'models-path': path.join(SRC_PATH, 'models'),
  'seeders-path': path.join(DATABASE_PATH, 'seeders'),
  'migrations-path': path.join(DATABASE_PATH, 'migrations')
}
